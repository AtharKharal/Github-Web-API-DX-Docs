# {key id}

API endpoints for {key id}.

## Endpoints


### [Get a public SSH key for the authenticated user](get-a-public-ssh-key-for-the-authenticated-user.md)

`GET` `{{baseUrl}}/user/keys/:key_id`

View extended details for a single public SSH key. Requires that you are authenticated via Basic Aut...


### [Delete a public SSH key for the authenticated user](delete-a-public-ssh-key-for-the-authenticated-user.md)

`DELETE` `{{baseUrl}}/user/keys/:key_id`

Removes a public SSH key from the authenticated user's GitHub account. Requires that you are authent...

