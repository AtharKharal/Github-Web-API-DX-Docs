# {gpg key id}

API endpoints for {gpg key id}.

## Endpoints


### [Get a GPG key for the authenticated user](get-a-gpg-key-for-the-authenticated-user.md)

`GET` `{{baseUrl}}/user/gpg_keys/:gpg_key_id`

View extended details for a single GPG key. Requires that you are authenticated via Basic Auth or vi...


### [Delete a GPG key for the authenticated user](delete-a-gpg-key-for-the-authenticated-user.md)

`DELETE` `{{baseUrl}}/user/gpg_keys/:gpg_key_id`

Removes a GPG key from the authenticated user's GitHub account. Requires that you are authenticated ...

