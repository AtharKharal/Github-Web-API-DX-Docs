# gitignore/templates

API endpoints for gitignore/templates.

## Endpoints


### [Get all gitignore templates](get-all-gitignore-templates.md)

`GET` `{{baseUrl}}/gitignore/templates`

List all templates available to pass as an option when [creating a repository](https://developer.git...


### [Get a gitignore template](get-a-gitignore-template.md)

`GET` `{{baseUrl}}/gitignore/templates/:name`

The API also allows fetching the source of a single template.
Use the raw [media type](https://devel...

