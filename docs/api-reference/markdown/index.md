# markdown

API endpoints for markdown.

## Endpoints


### [Render a Markdown document](render-a-markdown-document.md)

`POST` `{{baseUrl}}/markdown`

...


### [Render a Markdown document in raw mode](render-a-markdown-document-in-raw-mode.md)

`POST` `{{baseUrl}}/markdown/raw`

You must send Markdown as plain text (using a `Content-Type` header of `text/plain` or `text/x-markd...

