# {username}

API endpoints for {username}.

## Endpoints


### [Get team member (Legacy)](get-team-member-(legacy).md)

`GET` `{{baseUrl}}/teams/:team_id/members/:username`

The "Get team member" endpoint (described below) is deprecated.

We recommend using the [Get team me...


### [Add team member (Legacy)](add-team-member-(legacy).md)

`PUT` `{{baseUrl}}/teams/:team_id/members/:username`

The "Add team member" endpoint (described below) is deprecated.

We recommend using the [Add or upda...


### [Remove team member (Legacy)](remove-team-member-(legacy).md)

`DELETE` `{{baseUrl}}/teams/:team_id/members/:username`

The "Remove team member" endpoint (described below) is deprecated.

We recommend using the [Remove t...

