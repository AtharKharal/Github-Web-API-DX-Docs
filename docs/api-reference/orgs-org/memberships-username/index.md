# memberships/{username}

API endpoints for memberships/{username}.

## Endpoints


### [Get organization membership for a user](get-organization-membership-for-a-user.md)

`GET` `{{baseUrl}}/orgs/:org/memberships/:username`

In order to get a user's membership with an organization, the authenticated user must be an organiza...


### [Set organization membership for a user](set-organization-membership-for-a-user.md)

`PUT` `{{baseUrl}}/orgs/:org/memberships/:username`

Only authenticated organization owners can add a member to the organization or update the member's r...


### [Remove organization membership for a user](remove-organization-membership-for-a-user.md)

`DELETE` `{{baseUrl}}/orgs/:org/memberships/:username`

In order to remove a user's membership with an organization, the authenticated user must be an organ...

